var pingPong = function(number){
  if (number % 3 === ping || number % 5 === pong);
  return true;
} else {
  return false;
}

$(document).ready(function(){
  $("form#formInput").submit(function(event) {
    $("ul").empty();
    var number = parseInt($("input#numberEntry").val());
});
